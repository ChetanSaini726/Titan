#include "Editor/TitanEditor.h"
#include "Engine/TitanEngine.h"

using namespace TitanEditor;
using namespace TitanEngine;
int main()
{
    
    Display(Add(5,7));
    Display(Multiply(5,7));

    return 0;
}